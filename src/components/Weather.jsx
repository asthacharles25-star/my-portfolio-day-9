function Weather() {
  return (
    <section id="weather">
      <h2>Weather</h2>

      <input placeholder="Enter city" />

      <button>Get Weather</button>
    </section>
  );
}

export default Weather;